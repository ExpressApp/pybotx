dist: xenial

language: python

cache: pip

python:
    - "3.6"
    - "3.7"
    - "3.8"

install:
    - pip install poetry
    - poetry config virtualenvs.create false
    - poetry install --extras tests

script:
    - bash scripts/test